// "给你 2 枚相同 的鸡蛋"，题目意思是只有2枚鸡蛋，利用这仅有的2枚鸡蛋判断目标楼层。即，当第1枚鸡蛋碎了后，第2枚鸡蛋只能在未判定的楼层一层层从下往上尝试。class Solution {public:    int twoEggDrop(int n) {        if (n <= 2) return n;        if (n <= 5) return 3;        int k = findMin(1, n, n);        return k;    }    int findMin(int stpos, int endpos, int n) {        if (stpos > endpos) return -1;        if (stpos == endpos) return stpos;        if (stpos == endpos - 1) {            int sum = (1 + stpos) * stpos / 2;            if (sum >= n) return stpos;            return endpos;         }        int midpos = (stpos + endpos) / 2;        int sum = (1 + midpos) * midpos / 2;        if (sum == n) return midpos;        else if (sum < n) {            return findMin(midpos + 1, endpos, n);        } else {            return findMin(stpos, midpos, n);        }    }};